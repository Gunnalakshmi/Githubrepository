Git bash commands

git init 
git status
git add "filename"
git status 
git add "filename"
creating  an new file
git add .
(all untracked file and changes are added to the staging area)(dot meaning)

git commit -m "new files added"
('-' means message)

git config --global user.email "gunalakshmi215@gmail.com"

git config --global user.name "Gunnalakshmi"

git log
git diff

git ls-files
(it shows that what are the files are are there in the staging area)

git rm filename
(remove the file from staging area and local directory)

git rm --cached filename
(you want to keep the file in your harddisk byt you don't want git to trck this file anymore)

git branch 
git branch guna
git checkout
