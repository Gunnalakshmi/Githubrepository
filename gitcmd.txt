Git bash commands

git init 
git status
git add "filename"
git status 
git add "filename"
creating  an new file
git add .
(all untracked file and changes are added to the staging area)(dot meaning)

git commit -m "new files added"
('-' means message)

git config --global user.email "gunalakshmi215@gmail.com"

git config --global user.name "Gunnalakshmi"

git log
git diff
